/** Shared password / session rules — keep UI copy in lockstep with Better Auth. */
export const MIN_PASSWORD_LENGTH = 12;
/** Existing accounts may still have shorter passwords; sign-in must accept them. */
export const SIGN_IN_PASSWORD_MIN = 8;
export const RESET_TOKEN_MINUTES = 15;
export const SESSION_DAYS = 14;
